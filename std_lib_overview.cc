/** 
 * The standard library features include:
 * 
 * - C standard library
 * - Strings
 * - Regex
 * - I/O streams
 * - STL = a framework for containers
 * - numberic/mathematical operations
 * - Concurrency support (threads/locks)
 * - Template utilities
 * - "Smart pointers" for resource management (e.g. unique_ptr and shared_ptr)
 * - 
*/

/**
 * Here are some useful headers (not a complete list).
 * 
 * #include<algorithm> - copy(), find(), sort()
 * #include<array> - array
 * #include<chrono> - duration, time_point
 * #include<cmath> - sqrt(), pow()
 * #include<complex> - complex, sqrt(), pow()
 * #include<filesystem> - path
 * #include<forward_list> - forward list
 * #include<fstream> - fstream, ofstream, ifstream
 * #include<future> - future, promise
 * #include<ios> - hex, dec, scientific, fixed, defaultfloat.
 * #include<iostream> - istream, ostream, cin, cout
 * #include<map> - map, multimap
 * #include<memory> - unique_ptr, shared_ptr, allocator
 * #include<random> - default_random_engine, normal_distribution
 * #include<regex> - regex, match
 * #include<string> - string, basic_string
 * #include<set> - set, multiset
 * #include<stdexcept> - lenth_error, out_of_range, runtime_error
 * #include<thread> - thread
 * #include<unordered_map> - unordered_map, unordered_multimap
 * #include<utility> - move(), swap(), pair
 * #include<variant> - variant
 * #include<vector> - vector
 * #include<stdlib.c> - Headers from the C standard library
 */

int main(int argc, char* argv[]) {
  return 0;
}